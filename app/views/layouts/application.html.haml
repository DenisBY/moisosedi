!!!
%html
  %head
    %title Demo
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    %p= flash[:notice]
    -if user_signed_in?
      Вы вошли как #{current_user.owner}.
      #{link_to "Выйти", destroy_user_session_path, :method => :delete}
    = yield
